{% requireLogin %}

{#{% extends "_site/_layout.twig" %}#}
{% extends '_site/NewLayout' %}

{% set breadcrumbs = [entry] %}

{% block content %}

    {% include "_site/_snippet/_content/_defaultHeader" %}

    <div class="section section--default">
        <div class="container">
            <div class="flex flex-wrap -mx-4">
                <div class="w-full px-4 md:w-2/3">
                    Profile Details
                </div>
                <div class="w-full px-4 md:w-1/3">
                    <div class="p-6 bg-light">
                        <ul>
                            {% set editPage = craft.entries.section('editProfile').one() %}
                            {% if editPage|length %}
                                <li><a href="{{ editPage.getUrl() }}" class="link">{{ 'Edit Profile'|t }}</a></li>
                            {% endif %}
                            {% set editPassword = craft.entries.section('editPassword').one() %}
                            {% if editPassword|length %}
                                <li><a href="{{ editPassword.getUrl() }}" class="link">{{ 'Change password'|t }}</a></li>
                            {% endif %}
                            <li><a href="/logout" class="link">{{ 'Logout'|t }}</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}